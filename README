test rdphelper

 Set up git
  git config --global user.name "Your Name"
  git config --global user.email francois.marot@gmail.com
        
Next steps:
  mkdir rdphelper
  cd rdphelper
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:fmarot/rdphelper.git
  git push -u origin master
      

Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:fmarot/rdphelper.git
  git push -u origin master
      

